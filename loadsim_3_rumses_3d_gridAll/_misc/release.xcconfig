#include "../../_all_config/all.xcconfig"

HEADER_SEARCH_PATHS = $(HEADER_DEF) $(HEADER_TBB) $(HEADER_SNDFILE)
LIBRARY_SEARCH_PATHS = $(LIB_PATH_DEF) $(LIB_PATH_TBB) $(LIB_PATH_SNDFILE)
OTHER_LDFLAGS = $(LDFLAG_DEF_R) $(LDFLAG_TBB) $(LDFLAG_SNDFILE)

